import React from 'react';
import Headers from './container/ContainerHeader';

export default class MyHeader extends React.Component {
    render() {
        return (
          <Headers />
        );
    }
}
